# CRITICAL GIT DEPLOYMENT & REPOSITORY SYNC RULES — ANTIGRAVITY EXECUTION MODE

===============================================================================
IMPORTANT — READ GITREMEMBER FILE BEFORE ANY GIT ACTION
===============================================================================

This file is the AUTHORITATIVE Git deployment and repository synchronization
instruction for this project.

Whenever the user says:

    "Read GitRemember file"
    "Read git remember"
    "Follow GitRemember"
    "Use GitRemember rules"

Antigravity MUST first read this entire file and follow the repository mapping
defined here.

DO NOT make Git decisions based on assumptions.

===============================================================================
1. EXECUTION ENVIRONMENT
===============================================================================

ALL FOUR repository synchronization operations must be prepared from the
MONOREPO ROOT DIRECTORY.

The user will open Git Bash from the project ROOT directory.

Example:

PROJECT ROOT/
├── backend/
├── frontend/
├── web-frontend/
├── other project files
└── GitRemember file

Therefore, all generated Git commands must be executable from the ROOT
directory.

DO NOT require the user to manually open four different Git Bash windows.

DO NOT require the user to manually navigate into separate repositories.

Generate the complete command sets from the project ROOT.

===============================================================================
2. VERY IMPORTANT — DO NOT PERFORM GIT INSPECTION
===============================================================================

When the user explicitly asks:

    "Read GitRemember file"

Antigravity MUST NOT perform Git inspection commands before generating the
commands.

DO NOT run:

    git status
    git ls-files
    git branch
    git remote -v
    git remote
    git log
    git diff
    git diff --cached
    git rev-parse
    git branch -a
    git checkout
    git fetch
    git pull

DO NOT ask the user to run these commands.

DO NOT inspect Git state merely for confirmation.

The GitRemember file already defines the required repository mapping,
repository URLs, source folders, and target branches.

Use those rules directly.

===============================================================================
3. FOUR REQUIRED REPOSITORY TARGETS
===============================================================================

There are FOUR repositories.

Every valid project update must be synchronized according to the mapping below.

-------------------------------------------------------------------------------
A. PERSONAL / CENTRAL REPOSITORY
-------------------------------------------------------------------------------

Repository:

https://github.com/divyanscloudshiftsolutions/NFC-QR-code

SOURCE:

Complete monorepo root.

TARGET:

Complete project.

BRANCH:

main

RULE:

ALL legitimate project changes must be pushed to this repository.

This includes:

    backend/
    frontend/
    web-frontend/
    project-level files
    configuration
    documentation
    UI/UX implementation
    other legitimate project updates

The central repository must contain the COMPLETE project.

-------------------------------------------------------------------------------
B. WEB FRONTEND REPOSITORY
-------------------------------------------------------------------------------

Repository:

https://github.com/cloudshiftsolutions-dev/Web-Frontend---NFC-QR-code-Management

SOURCE:

web-frontend/

TARGET:

The CONTENTS of web-frontend/ must appear directly at the ROOT of the
dedicated repository.

Required structure:

    Web-Frontend---NFC-QR-code-Management/
    ├── src/
    ├── public/
    ├── package.json
    ├── index.html
    ├── vite.config.*
    └── other Web Frontend files

NOT:

    Web-Frontend---NFC-QR-code-Management/
    └── web-frontend/
        └── src/

BRANCH:

webfrontend-branch

NEVER synchronize:

    backend/
    frontend/
    unrelated project files
    monorepo wrapper folders

-------------------------------------------------------------------------------
C. BACKEND REPOSITORY
-------------------------------------------------------------------------------

Repository:

https://github.com/cloudshiftsolutions-dev/Backend---NFC-QR-code-Management

SOURCE:

backend/

TARGET:

The CONTENTS of backend/ must appear directly at the ROOT of the dedicated
repository.

Required structure:

    Backend---NFC-QR-code-Management/
    ├── src/
    ├── prisma/
    ├── package.json
    ├── tsconfig.json
    └── other Backend files

NOT:

    Backend---NFC-QR-code-Management/
    └── backend/
        └── src/

BRANCH:

main

NEVER synchronize:

    frontend/
    web-frontend/
    unrelated project files

-------------------------------------------------------------------------------
D. REACT NATIVE FRONTEND REPOSITORY
-------------------------------------------------------------------------------

Repository:

https://github.com/cloudshiftsolutions-dev/Frontend---NFC-QR-code-Management

SOURCE:

frontend/

TARGET:

The CONTENTS of frontend/ must appear directly at the ROOT of the dedicated
repository.

Required structure:

    Frontend---NFC-QR-code-Management/
    ├── src/
    ├── assets/
    ├── App.tsx
    ├── package.json
    ├── app.json
    ├── eas.json
    └── other React Native files

NOT:

    Frontend---NFC-QR-code-Management/
    └── frontend/
        └── src/

BRANCH:

Use the existing React Native deployment branch defined by the GitRemember
rules.

DO NOT invent a branch name.

NEVER synchronize:

    backend/
    web-frontend/
    unrelated project files

===============================================================================
4. CHANGE → REPOSITORY MAPPING
===============================================================================

Use this exact mapping.

    backend/**
        → Central Repository
        → Backend Repository

    frontend/**
        → Central Repository
        → React Native Frontend Repository

    web-frontend/**
        → Central Repository
        → Web Frontend Repository

    project-root files
        → Central Repository ONLY

Therefore:

CENTRAL REPOSITORY
    = COMPLETE PROJECT

BACKEND REPOSITORY
    = backend/ CONTENTS ONLY

REACT NATIVE REPOSITORY
    = frontend/ CONTENTS ONLY

WEB FRONTEND REPOSITORY
    = web-frontend/ CONTENTS ONLY

===============================================================================
5. IMPORTANT — GENERATE FOUR SEPARATE COMMAND BLOCKS
===============================================================================

Whenever the user asks to synchronize/push the project, generate FOUR
separate copyable command blocks.

The order MUST be:

    1. Personal / Central Repository
    2. Web Frontend Repository
    3. Backend Repository
    4. React Native Frontend Repository

Each repository MUST have its own separate copy-code block.

DO NOT combine all repositories into one command block.

DO NOT make the user manually construct commands.

===============================================================================
6. EACH COMMAND BLOCK MUST CONTAIN
===============================================================================

Every repository command block must clearly contain:

    Repository name
    Repository URL
    Source directory
    Target branch
    Exact synchronization commands
    Git add/staging command
    Git commit command
    Git push command

The commit message must be meaningful and specific to the actual changes.

Example:

    git commit -m "fix: resolve QR auto-scanning in check-in flow"

DO NOT use meaningless messages such as:

    update
    changes
    fixed
    latest
    test

===============================================================================
7. PERSONAL / CENTRAL REPOSITORY COMMANDS
===============================================================================

The central repository is the normal monorepo Git repository.

Its command block must synchronize the COMPLETE project.

The command block should follow this structure:

    # Personal / Central Repository
    # Repository:
    # https://github.com/divyanscloudshiftsolutions/NFC-QR-code
    # Branch: main

    git add <required project changes>
    git commit -m "<appropriate commit message>"
    git push origin main

If the task contains multiple legitimate project changes, include all relevant
project changes required for the central repository.

The central repository MUST receive all project updates.

===============================================================================
8. WEB FRONTEND REPOSITORY COMMANDS
===============================================================================

The Web Frontend repository receives ONLY:

    web-frontend/

The resulting dedicated repository must expose the contents directly at ROOT.

The synchronization method must preserve this root-level structure.

Preferred approach:

    git subtree

or another equivalent Git mechanism that correctly publishes:

    web-frontend/

as:

    repository-root/

DO NOT publish:

    web-frontend/

as a nested folder.

The command block must clearly identify:

    Repository:
    https://github.com/cloudshiftsolutions-dev/Web-Frontend---NFC-QR-code-Management

    Source:
    web-frontend/

    Branch:
    webfrontend-branch

The command block must contain the required synchronization, commit, and push
commands.

===============================================================================
9. BACKEND REPOSITORY COMMANDS
===============================================================================

The Backend repository receives ONLY:

    backend/

The resulting dedicated repository must expose backend contents directly at
repository ROOT.

Use subtree or an equivalent synchronization mechanism when operating from
the monorepo root.

Do NOT create:

    backend/backend/

Do NOT publish unrelated project folders.

The command block must clearly show:

    Repository:
    https://github.com/cloudshiftsolutions-dev/Backend---NFC-QR-code-Management

    Source:
    backend/

    Branch:
    <existing backend deployment branch defined by GitRemember>

Then provide the synchronization, commit, and push commands.

===============================================================================
10. REACT NATIVE FRONTEND REPOSITORY COMMANDS
===============================================================================

The React Native repository receives ONLY:

    frontend/

The resulting dedicated repository must expose frontend contents directly at
repository ROOT.

Use subtree or an equivalent synchronization mechanism.

Do NOT create:

    frontend/frontend/

Do NOT publish:

    backend/
    web-frontend/
    unrelated files

The command block must clearly show:

    Repository:
    https://github.com/cloudshiftsolutions-dev/Frontend---NFC-QR-code-Management

    Source:
    frontend/

    Branch:
    <existing React Native deployment branch defined by GitRemember>

Then provide the synchronization, commit, and push commands.

===============================================================================
11. ONLY PUSH RELEVANT DEDICATED REPOSITORIES
===============================================================================

If today's changes affect only:

    web-frontend/

Then:

CENTRAL REPOSITORY
    → PUSH

WEB FRONTEND REPOSITORY
    → PUSH

BACKEND REPOSITORY
    → DO NOT PUSH

REACT NATIVE REPOSITORY
    → DO NOT PUSH

If today's changes affect:

    backend/
    web-frontend/

Then:

CENTRAL REPOSITORY
    → PUSH BOTH

WEB FRONTEND REPOSITORY
    → PUSH web-frontend ONLY

BACKEND REPOSITORY
    → PUSH backend ONLY

REACT NATIVE REPOSITORY
    → DO NOT PUSH

Never push an unchanged dedicated project merely because the repository
exists.

===============================================================================
12. NEVER MIX PROJECT SCOPES
===============================================================================

WEB FRONTEND REPOSITORY:

    ONLY web-frontend/

BACKEND REPOSITORY:

    ONLY backend/

REACT NATIVE REPOSITORY:

    ONLY frontend/

CENTRAL REPOSITORY:

    COMPLETE PROJECT

NEVER push:

    backend → Web Frontend repository
    frontend → Web Frontend repository
    web-frontend → Backend repository
    backend → React Native repository
    entire monorepo → dedicated repository

===============================================================================
13. COMMIT MESSAGE RULE
===============================================================================

Every repository must receive a proper commit message.

The commit message should describe the actual implementation.

Examples:

    fix: resolve check-in QR email delivery flow

    fix: enable full-frame webcam QR auto-scanning

    fix: prevent duplicate QR email dispatch

    fix: improve responsive tables layout

    feat: update check-in QR verification workflow

Use the same logical change description across repositories when the same
feature is synchronized to multiple repositories.

===============================================================================
14. NO UNNECESSARY TEST EMAILS
===============================================================================

When synchronizing code related to email, QR, check-in, or notifications:

DO NOT send test emails merely to verify Git synchronization.

DO NOT create unnecessary test customers.

DO NOT generate unnecessary QR emails.

Git synchronization and application testing are separate operations.

===============================================================================
15. EAS / EIS RULE
===============================================================================

EAS/EIS commands may ONLY be executed from:

    frontend/

or the exact React Native directory containing:

    eas.json

Never run EAS/EIS commands from:

    project root
    backend/
    web-frontend/

===============================================================================
16. OUTPUT FORMAT — MANDATORY
===============================================================================

When the user asks:

    "Read GitRemember file"

and requests Git synchronization commands, respond with a concise execution
plan followed by separate copy-code blocks.

Use this structure:

    ## Git Synchronization Plan

    Changed scopes:
    - backend/
    - web-frontend/
    - etc.

    The following repositories require synchronization:
    - Personal / Central
    - Web Frontend
    - Backend
    - React Native

Then provide separate blocks.

-------------------------------------------------------------------------------
COPY BLOCK 1 — PERSONAL / CENTRAL
-------------------------------------------------------------------------------

```bash
# Personal / Central Repository
# Repository: https://github.com/divyanscloudshiftsolutions/NFC-QR-code
# Source: Complete monorepo
# Branch: main

<commands>

git commit -m "<proper commit message>"
git push origin main