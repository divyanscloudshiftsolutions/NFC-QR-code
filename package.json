{
  "name": "nfc-bar-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "npm install --prefix backend --legacy-peer-deps",
    "build": "npm run install-backend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
