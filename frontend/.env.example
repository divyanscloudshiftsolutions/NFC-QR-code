# Production Railway API Endpoint
# Configure this environment variable to point your local Expo app directly to your deployed Railway backend.
# The Metro packager will automatically bind this value during local runs and APK compilations.
EXPO_PUBLIC_API_URL="https://nfc-qr-code-production.up.railway.app/api"
